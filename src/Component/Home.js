import React from "react"
import { Container, Row, Col, Navbar, Nav, Form, Button, FormControl } from "reactstrap"
import NavbarPage from "./Layout/Navbar"

const Home = () => {
    return (
        <Container fluid>
            <NavbarPage />
        </Container>
    )
}

export default Home;